#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
   ios::sync_with_stdio(0);
   cin.tie(0);
   int a, b;
   scanf("%d%d", &a, &b);
   printf("%d - %d", a, b);
   return 0;
}
